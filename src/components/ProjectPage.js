import React, { useState } from "react";

const ProjectPage = () => {
    return (
        <div>
            {/* {project.map((project) => (
                <div>{project.name}</div>
            ))} */}
        </div>
    );
};

export default ProjectPage;
